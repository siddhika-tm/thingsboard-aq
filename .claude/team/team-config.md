# Team config — Airlinq IoT Platform

Generated by `/init-team`. This file + the overlays in `./overlays/` are the ONLY source of project facts for the virtual team. Commit this whole `.claude/team/` tree with the code.

## 1. Project
- **Name:** Airlinq IoT Platform
- **Description:** ThingsBoard 4.4.0-SNAPSHOT fork (Java 25 + Angular 20, Maven reactor) rebranded for Airlinq, running a container-terminal demo on a single VPN-only CentOS server.
- **Repo root marker:** this file lives at `.claude/team/team-config.md` relative to repo root. All paths below are repo-relative.

## 2. Mode & roster
- **Mode:** dev   <!-- dev | automation | architect | devops -->
- **Roster (dev):** technical-architect, developer, code-reviewer, tester
- **Roster (automation):** automation-developer, automation-code-reviewer, qe-tester
- **Roster (architect):** architect
- **Roster (devops):** devops-engineer, devops-tester
- **Orchestrator:** jarvis (always)
- Roster overrides (add/remove roles only with a documented reason): none

## 3. Protected branches & commits
- **Protected branch(es):** master (never commit directly; PRs target master)
- **Feature branch naming:** `feature/<short-description>` (lowercase, hyphenated; add a ticket id when one exists, e.g. `feature/AQ-123-short-description`); `bugfix/`, `hotfix/` likewise
- **Commit convention:** Conventional Commits (`<type>(<scope>): <imperative subject>`, body explains why; footer `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>` when an agent authored it)
- **Commit policy:** manual   <!-- manual (default) | agent-after-g4 -->  Agents never commit or push; the human reviews and commits.

## 4. Gates
Standard gates G1–G4 per the `team-protocol` skill. Project-specific ADDITIONAL gates (may add, never remove):
none

## 5. Document paths
- Requirements: docs/requirements/          <!-- default: docs/requirements/ -->
- Design docs (dev mode): docs/design/      <!-- default: docs/design/ -->
- Plans: docs/plans/                        <!-- default: docs/plans/ -->
- ADRs (dev mode): docs/adr/                <!-- default: docs/adr/ -->
- Existing design specs from earlier work: `docs/superpowers/specs/` (gitignored, local reference only)

## 6. Memory paths
Per the `memory-methodology` skill. Default (recommended): `.claude/team/memory/`. Override: default

## 7. Open questions
none

## 8. Model policy
- Every team member runs on **Opus**. The dev-team plugin (1.5.3) ships developer, code-reviewer and tester on Sonnet, so this repo carries project-level overrides in `.claude/agents/{developer,code-reviewer,tester}.md` — identical prompts, `model: opus`. Project agents take precedence over plugin agents; re-check after a plugin upgrade and refresh the copies if the upstream prompt changed.
