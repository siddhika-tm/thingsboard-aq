# Role memory — tester

Role-private learnings index. One line per learning, newest first; prune entries that stopped being true.
- A small CROSS-server pixel delta proves nothing until the SAME server is compared against itself: a 34 px / 7x7 box read as a dark regression on two routes, but the same-server control reproduced it identically (live alarm-badge churn; `/alarms` rows also moved 6->5 unprompted) and the re-run cross-server pass measured 0. `2026-09-08-dnew3-final-ac30.md`
- Measure a per-route pixel NOISE FLOOR first and let it pick the targets: 4 table routes 0 px, `/home` self-differs 65,197 px / 4.07% (animating widgets + telemetry) - exclude it, never explain it away. `2026-09-08-dnew3-final-ac30.md`
- `p.goto()` per route reloads the SPA and races the spinner - a 97% "difference" with the SAME huge bounding box on EVERY route is that artifact, not a defect. Wait on spinner-hidden AND (rows>0 || empty-state || widgets>0), and record `rows`/`spinnerVisible` per capture so a blank screenshot cannot become evidence. `2026-09-08-dnew3-final-ac30.md`
- `playwright-core.evaluate()` silently returns `undefined` for a STRING arrow function (surfaces as a `length` TypeError far away) - pass a real function. `2026-09-08-dnew3-final-ac30.md`
- Put the post-login `loggedIn` assertion BEFORE any screenshot write and make it throw: concurrent runs against one dev server transiently fail login (`Unhandled error code Unknown`) and otherwise leave 7 authoritative-looking PNGs of the login page. `2026-09-08-dnew3-final-ac30.md`
- `ng serve` already reads `proxy.conf.js` from `angular.json`, so the MAIN tree's tracked copy never needs editing - pass a modified copy via `--proxy-config` and the LF->CRLF dirty-file trap never happens (better than reverting after). `2026-09-08-dnew3-final-ac30.md`
- `matches('.tb-dark') && !matches('body.tb-dark')` is the exact affected-set probe for a selector narrowing; sweeping it over 9 authenticated + 5 auth routes x 2 themes turned "did anything else regress?" into an enumeration (authenticated affected=0, auth affected=1). `2026-09-08-dnew3-final-ac30.md`
- A narrowing fix preserves the other theme via a DESCENDANT rule matching through the ancestor: the auth div's `.tb-dark .mat-app-background` match is false in light (paints `--aq-auth-bg`) and true in dark via `<body class=tb-dark>` (paints `rgb(10,13,18)`). Prove both directions per theme with `Element.matches()` + PNG decode. `2026-09-08-dnew3-final-ac30.md`
- A frozen-block DIGEST change is not a content change: hash moved but the sorted declaration multiset diffed to ZERO (75/75 name+value) and the raw diff was one selector wrapper. Report the multiset as proof, the hash as an index, and publish the counting convention (my 75 vs the item's 82 is convention, not content). `2026-09-08-dnew3-final-ac30.md`
- Attribute every pixel difference to a diff hunk before calling it a regression: dark `/login` differed 39,379 px, and the mask's doubled text + `git diff` pinned it to the `OPERATOR CONSOLE` strip removal and `login.component.scss`, separate approved work in the same item. `2026-09-08-dnew3-final-ac30.md`
- Reconcile a `node_modules` count before reporting it: post-teardown `ls | wc -l` gave 918 vs 920 and looked like deletion; `ls -A` restored 920 (`.bin`, `.yarn-integrity`). Remove a junction with `(Get-Item $j).Delete()`. `2026-09-08-dnew3-final-ac30.md`
Team-relevant items go to the sign-off as `**Team-learning candidate:**` for Jarvis to promote.

- Serve the pristine HEAD **alongside** the worktree (second `git worktree` + `node_modules` via a Windows **junction**, not a fresh install) and diff mechanically: it turned AC-3 into a hard result (49 dark tokens byte-identical, 0 changed, 14 pure additions) and settled three "is this pre-existing?" calls. Delete the junction BEFORE `git worktree remove` or the real `node_modules` can go with it. `2026-09-08-g3-final-blue-light.md`
- A contrast-sweep identity key must NOT embed the resolved backdrop: a deliberate 1-unit `--aq-surface` change made all 25 pre-existing failures read as both "new" and "resolved". Key on element identity (tag+classes+text) and compare the RATIO. `2026-09-08-g3-final-blue-light.md`
- An element with **no direct text node** is never an ink: `.tb-user-info` (2.68:1) and `section.tb-timewindow` (rgb(82,92,110)) both inherit a colour they never paint - the glyphs are child spans at 8.87-14.11:1. Require a `nodeType===3` non-empty child before measuring. `2026-09-08-g3-final-blue-light.md`
- For "contrast at every point along a span" over a gradient, take each column's **darkest** pixel as the backdrop and compare to the known ink; a modal-vs-most-distant read collapses to ~1.1:1 inside antialiased glyphs and fabricates a FAIL. `2026-09-08-g3-final-blue-light.md`
- `grep -c '!important'` over added lines is not the AC-23 check: 27 hits, 0 declarations (prose + one QUOTATION of the rule the fix must not fight). Classify by declaration position, then read every survivor's context. `2026-09-08-g3-final-blue-light.md`
- Auth error styling has two paths: a wrong password returns a server error -> Material **snackbar**, inputs stay `ng-valid`, so `.aq-error`/`.aq-invalid` never render. Client-side validation (clear field, dispatch `input`+`blur`) is what shows the `#c23333` text / `#dc4848` border split. `2026-09-08-g3-final-blue-light.md`
- `stateObject` is the breadcrumb **stack**, not the dashboard's state **count**: a 3-state dashboard still renders the `<h1>` branch until you drill into a child state. Before accepting a `window.ng`-synthesised branch, enumerate real data that already reaches it (`GET /api/tenant/dashboards` -> `configuration.states`). `2026-09-08-g3-final-blue-light.md`
- `python3` here cannot open `/d/tmp/...` (needs `D:/tmp/...`) even where `ls` works; Write is sandboxed to the repo so scripts belong in `.claude/team/artifacts/<run>/` (untracked); heredocs mangle backslashes - put any script with a `\` in a file; Playwright `browser_evaluate` `filename:` writes to the REPO ROOT, so `mv` it or it dirties `git status`. `2026-09-08-g3-final-blue-light.md`
- A static review at ZERO findings proves nothing about runtime: lint green, `BUILD SUCCESS` and a clean reviewer pass coexisted with FOUR routes rendering no left menu at all. Sweep every route the changed navigation can reach. `2026-09-06-g3-run2-left-menu.md`
- Reshaping a shared data model breaks unguarded consumers that live OUTSIDE the diff: `router-tabs.component.ts:113` does `found.pages.filter()` unguarded while `findRootSection`:151 guards the same property; flattening the menu left a `type:'link'` section with no `pages` and blanked the whole side menu. Grep every consumer of a reshaped property. `2026-09-06-g3-run2-left-menu.md`
- Read the SCSS selector FIRST, then query that class by name: measuring the CDK ancestor `.tb-popover-inner` instead of `.tb-toggle-menu-items` produced three false findings (bg/padding/radius) that were all exact on the right element. `2026-09-06-g3-run2-left-menu.md`
- `width: auto` on an `inline-flex` anchor shrink-wraps: the rail `<li>` was the correct 48px while the anchor inside measured 34px. Walk the box chain anchor->shell, never one level. `2026-09-06-g3-run2-left-menu.md`
- Separate "binding not set" from "host wiped": three routes with zero `aria-current` looked like an aria defect but were collateral of a larger crash; diffing `aria-current` against pre-existing `tb-active` across all routes (zero mismatches) cleared the binding. `2026-09-06-g3-run2-left-menu.md`
- `ps -W` prints MEMORY, not PIDs, so `taskkill //PID` says "not found" while the server keeps serving; use `Get-CimInstance Win32_Process` filtered on CommandLine, then verify the port is closed. `2026-09-06-g3-run2-left-menu.md`
- A heredoc containing an apostrophe breaks `bash <<'EOF'`; write long reports with the Write tool. `2026-09-06-g3-run2-left-menu.md`
- `.tb-dark { }` does NOT match in light — the mirror of the `.tb-default` lesson, and it does not invert. `styles.scss` opens a "DARK-ONLY additions" block at :1620; the AC-24 lift (:1683), the `:focus-visible` ring (:1697) and the sidebar hover/active colours (:1669,:1704) all sit inside it, so they work in dark and vanish in light. Resolve the enclosing selector by walking BRACE NESTING, never by the nearest heading comment. `2026-09-06-g27-left-menu.md`
- Run every theme-sensitive criterion in BOTH themes before writing a verdict: dark passed AC-24 while light failed it — either theme alone yields a wrong finding. `2026-09-06-g27-left-menu.md`
- Injection proof cannot distinguish "outranked" from "scoped out" — both look identical (`el.matches(':focus-visible')===true` yet `outline-style:none`, and the injected copy paints). The brace walk is what separates them. `2026-09-06-g27-left-menu.md`
- `event.defaultPrevented===false` after a real dispatched KeyboardEvent proves a handler never ran, without reading the TS — pair it with `ng.getComponent()` to show every guard should have passed. `2026-09-06-g27-left-menu.md`
- Server-persisted settings make probes non-idempotent: `menuCollapsed` is stored per user, so a blind "click the pin" collapsed the menu on the second run. Assert the TARGET state and toggle only if needed. Restore it at teardown (PUT `/api/user/settings`, then read back). `2026-09-06-g27-left-menu.md`
- One Playwright `--user-data-dir` cannot serve two concurrent processes ("Target page, context or browser has been closed"). Parameterise it (`process.env.UDD`) from the start. `2026-09-06-g27-left-menu.md`
- Serialise `yarn start`, `yarn lint` and `mvn install` — lint dies exit 134 "Reached heap limit" at 8 GB if Maven compiles alongside it. `2026-09-06-g27-left-menu.md`
- `isVisible()` round-trips are too slow to time a ~300 ms animation (made a 300 ms close-delay read as 3 s, nearly a false AC-29 failure). Sample in-page with `requestAnimationFrame` into `window.__samples`. `2026-09-06-g27-left-menu.md`
- Scope "does every row navigate" to `:scope > li > tb-menu-link > a`; the looser `li > tb-menu-link a` also matches collapsed sub-list rows (`visibility:hidden`) and reports false click-timeout failures. `2026-09-06-g27-left-menu.md`
- Regex-parsing the 1139-line nested menu model for route parity is NOT evidence (it invented LOST `/alarms`,`/notification`). Use the RENDERED menu per authority, and drive the real `MenuService.homeSections()` via `ng.getComponent` for Home-card (D9) checks. `2026-09-06-g27-left-menu.md`
- Criterion wording goes stale against later decisions (AC-11 "five sub-menus" vs D2's six; AC-22's "Monitor tile" vs headings being `display:none` in the rail). If the measurable intent is met, record PASS + a note to Jarvis — not a finding. `2026-09-06-g27-left-menu.md`

- A custom widget can render perfectly and still be wrong: replacing a widget TYPE does not migrate its DATASOURCE. The fleet bar kept the old progress-bar `yard_health_pct` key; a label-matching controller (`label.indexOf('align')>=0`) bucketed the 54.5% into the *aligned count* slot and drew a confident full-width bar. Compare rendered numbers to independently derived ground truth, never to the config. `2026-09-05-g3-run3-dashboard-task10.md`
- Test the DEPLOYED app directly via Playwright (`channel: msedge`) instead of proxying `yarn start` - faster, and sidesteps the `proxy.conf.js` LF->CRLF dirty-file trap. But the prod bundle STRIPS `window.ng` and `window.echarts`, so every `ng.getComponent` probe returns "no ng"; this build renders ECharts as **SVG, not canvas**, so read `svg path[stroke-dasharray]` / `stroke-width` / `stroke="var(--aq-*)"` instead - rendered output is stronger evidence than stored config. `2026-09-05-g3-run5-dashboard-final.md`
- Prove a SCOPED selector in BOTH directions: D23a scoped the pill to `mat-toolbar .tb-timewindow`, so assert the toolbar half HAS it and the in-widget half is UNCHANGED (split by `closest(mat-toolbar)`). Likewise assert sibling subtrees have ZERO bordered elements. A one-directional pass hides a leak. `2026-09-05-g3-run5-dashboard-final.md`
- Edge logs a COOP "origin was untrustworthy" **error** on every plain-HTTP page load - environmental, not an app defect. Filter by text and say so in the report. And `color(srgb r g b / a)` is just how Edge serializes `color-mix`; convert to hex before calling it a token mismatch (`0.639216 0.0941176 0.164706` = `#a3182a`). `2026-09-05-g3-run5-dashboard-final.md`
- `.tb-widget-loading` is permanently mounted and toggled via Tailwind `!hidden` - a `mat-spinner`-presence check reports every widget as spinning. Assert the `!hidden` class. Likewise `'tb-widget-container, .tb-widget'` double-counts every widget (57 for a 19-widget dashboard); use `tb-widget.tb-widget` and find value cards by `widget-type-system-cards-value_card`. `2026-09-05-g3-run3-dashboard-task10.md`
- `window.echarts` does not exist in this build: reach the instance by walking `ng.getComponent(<tb-time-series-chart-widget>)` for an object having both `getOption`/`setOption`. The `markLine` sits on a SEPARATE unnamed 12th series - a `s['name']` scan KeyErrors and would produce a false "no threshold rule" finding. `2026-09-05-g3-run3-dashboard-task10.md`
- Token `raw` vs `resolved` diverge: `--aq-error-tint-strong` is UNDEFINED (`getPropertyValue` -> `''`) yet a probe resolves it to `rgb(0,0,0)`, so `var(--undefined, var(--fallback))` silently takes the fallback. Check raw emptiness before asserting a token-derived colour. `2026-09-05-g3-run3-dashboard-task10.md`
- Node/yarn are NOT on PATH; the maven-downloaded pair lives at `ui-ngx/target/node/node.exe` + `node target/yarn/dist/bin/yarn.js start`. A bare `yarn start` fails and mimics an environment blocker. And `sed -i` leaves `proxy.conf.js` dirty via LF->CRLF even when byte-identical - revert with `git checkout --`. `2026-09-05-g3-run3-dashboard-task10.md`

- A 1px header-band delta is usually browser table rounding, not a CSS bug: in `display: table` a row's `height` is a MINIMUM and the cell border is distributed +-1px per page. Prove it by reverting to the upstream value - if the split persists at every height, it is not the diff. `2026-09-05-g3-run2-grid-fidelity.md`
- A route-level `querySelector` can match the PREVIOUS page's still-mounted widget (a "/users header is 9.5px" alarm was the home dashboard's alarms widget). Assert `closest('tb-entities-table')` before reporting any per-route measurement. `2026-09-05-g3-run2-grid-fidelity.md`
- Locate table cells by HEADER ORDINAL, never by `columns[]` index arithmetic - a leading selection/action column shifts it and silently reports the wrong column. `2026-09-05-g3-run2-grid-fidelity.md`
- Driving the real component via `ng.getComponent(host)` exercises states live data never produces (all demo alarms are CRITICAL, so warning/neutral tones would go untested). To stub an rxjs Observable there, clone a real instance's prototype - a hand-rolled `{subscribe,pipe}` throws `Unable to lift unknown Observable type`. `2026-09-05-g3-run2-grid-fidelity.md`
- A rule that EXISTS may never APPLY: 4 of 5 G3 findings were correct CSS outranked by a pre-existing deeper-nested selector (`.tb-default .mat-mdc-table .mat-mdc-header-row` (0,3,0) beats `.tb-default .mat-mdc-header-row` (0,2,0); `!important` does not rescue lower specificity). Assert `getComputedStyle`, never stylesheet text; prove the diagnosis by injecting the same declaration at higher specificity and re-measuring. `document.styleSheets.cssRules` is empty for Angular component sheets - use the DOM experiment. `2026-09-05-g3-run1-grid-fidelity.md`
- Selector families are build-specific: this build renders `<mat-row>`, not `<tr class="mat-mdc-row">` - a `tr`-only selector times out and mimics "page never loaded". Always `'mat-row, tr.mat-mdc-row'`. Scope drawer/tab probes to `.mat-mdc-tab-body-active` or you silently measure the background grid. `2026-09-05-g3-run1-grid-fidelity.md`
- Before raising a dark-leak or similar probe hit, confirm it is VISIBLE (`opacity`/`visibility`) and that the diff touches it - `mat-button-toggle-focus-overlay` at `opacity:0` is a false positive. `2026-09-05-g3-run1-grid-fidelity.md`
- Lint delta by file:line:rule, but tolerate pure line shifts: equal counts hid 3 entries that had merely moved +4 lines under inserted markup. `2026-09-05-g3-run1-grid-fidelity.md`
- A known pre-existing env failure can mask a criterion: `mvn install` aborted at `netty-mqtt` (Docker absent) BEFORE reaching ui-ngx, proving nothing about the diff - re-run with `-DskipTests`, and read BUILD SUCCESS from the log (an `echo $?` after a redirect reports the echo). `2026-09-05-g3-run1-grid-fidelity.md`
- Weigh reachability before severity: the suspected pager-clamp gap (candidate High) PASSED - every reachable shrink path resets `pageIndex=0` upstream. `2026-09-05-g3-run1-grid-fidelity.md`
- Write expected results from the IMPLEMENTED code, not from the AC text: on the grid work, AC-20's bold range was deliberately dropped, the count badge hides at total 0, and the neutral chip uses `--aq-hover` not a tint — three false findings avoided. `2026-09-05-g27-test-case-drafting.md`
- Never trust a stated blast radius: `tb-entities-table` has **46** consumers (39 routed + 7 wrapper in drawer tabs/dialogs), not the "7 additional"/"10+" the requirement and design claim. Delegate the inventory to a search subagent; it reshapes the regression tier.
- Applicability flags are DERIVED, not declared: effective selection = `selectionEnabled && enabledGroupActionDescriptors.length`, and Delete is auto-appended when `entitiesDeleteEnabled` is true. `displayPagination=false` is set nowhere, so pager criteria never get an N/A. Mark N/A from observed DOM, not from the config flag.
- Probe environment prerequisites while DRAFTING, but check team memory before declaring a blocker: JDK 25 + Maven 3.9.9 ARE present at `d:/tmp/tools/`, merely off the default PATH (`export JAVA_HOME=/d/tmp/tools/jdk-25.0.4.1+1; export PATH="$JAVA_HOME/bin:/d/tmp/tools/apache-maven-3.9.9/bin:$PATH"`). My earlier "JDK 22 only, no Maven" note was WRONG and cost a false blocker at G2.7.
- When the implementation knowingly deviates from an AC, write BOTH cases (actual behaviour + as-written failure) and route the ruling to the human; never silently assert one version.
- Contrast on tinted chips must be sampled COMPOSITED: cell-level `!important` hover/selected overlays sit under the chip tint, so static ratio recomputation is insufficient — 5 tones x 2 themes x 3 row states = 30 samples.
- `ui-ngx/proxy.conf.js` is git-TRACKED and not ignored, but must be pointed at the dev server to test; always finish a run with an explicit "working tree clean, proxy.conf.js unmodified" check.
- Structural proof is not visual proof: a missing `instanceof` in `columnsUpdated()` builds clean, lints clean and passes every grep while `mat-table` throws `Could not find column` at runtime. Always open a browser for a "does it render" case first.
